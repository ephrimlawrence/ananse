menu welcome {
    display "Enter first name"
    input first_name
    goto products
    action wemove(para: SFSDF) as action_variable

    menu submenu {
        display "Enter first name"
        option 1 "First choice" -> next_menu
        option 2 "second choice" action wemove(para: SFSDF) as action_variable
        input first_name
        goto products
    }
}
