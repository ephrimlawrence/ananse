{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "crystal: debug current file",
      "preLaunchTask": "crystal: build current file (debug)",
      "program": "${workspaceFolder}/compiler/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}/compiler",
      "initCommands": [
        "command script import compiler/crystal_formatters.py"
      ]
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "crystal: debug compiler",
      "preLaunchTask": "crystal: build compiler (debug)",
      "program": "${workspaceFolder}/compiler/compiler",
      "args": [],
      "cwd": "${workspaceFolder}/compiler",
      "initCommands": [
        "command script import compiler/crystal_formatters.py"
      ]
    }
  ]
}
